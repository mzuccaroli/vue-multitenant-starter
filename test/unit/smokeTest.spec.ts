// import { shallowMount } from "@vue/test-utils";
// import VaStatsWidget from "@/components/molecules/VaStatsWidget.vue";
// import router from "../../src/router";

describe("Smoke Test", () => {
  it("should run vitest", () => {
    expect(true);
  });
  // it("shoud load and test a ts file (router)", () => {
  //   expect(router).toBeDefined();
  // });
  // it("shoud load and test a vue component (VaStatsWidget)", () => {
  //   const wrapper = shallowMount(VaStatsWidget, {});
  //   expect(wrapper.html()).toContain("<div");
  // });
});
